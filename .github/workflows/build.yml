name: Build APK
on:
  push:
    branches: [ main, master ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
      - uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ hashFiles('**/*.gradle*') }}
      - run: chmod +x ./gradlew
      - run: ./gradlew assembleDebug
      - uses: actions/upload-artifact@v4
        with:
          name: GhadaGame-APK
          path: app/build/outputs/apk/debug/app-debug.apk
```
```
5. اضغط "Commit changes" الأخضر
```

---

## الخطوة 4 — تحميل APK ✅
```
1. اضغط "More" (الذي تراه في صورتك)
2. اضغط "Actions" ⚡
3. انتظر 3-5 دقائق حتى يظهر ✅ أخضر
4. اضغط على اسم الـ Build
5. اسحب للأسفل → Artifacts
6. اضغط "GhadaGame-APK" ← APK جاهز! 🎮
